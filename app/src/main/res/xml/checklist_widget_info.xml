<?xml version="1.0" encoding="utf-8"?>
<!-- The size floors are deliberately tiny (40dp, i.e. one cell) and no maximum
     is declared, so the launcher's grid is the only limit on how small or large
     the widget can be dragged. targetCell* only picks the size it is first
     dropped at. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:configure="com.rauaap.remember.WidgetConfigActivity"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_checklists"
    android:minHeight="40dp"
    android:minResizeHeight="40dp"
    android:minResizeWidth="40dp"
    android:minWidth="40dp"
    android:previewLayout="@layout/widget_preview"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="3"
    android:targetCellWidth="3"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen"
    android:widgetFeatures="reconfigurable|configuration_optional" />
